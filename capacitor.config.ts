import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.za.webgooru.app',
  appName: 'expense-tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
